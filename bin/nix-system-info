#!/usr/bin/env python3.9
import json
import subprocess

config = json.loads(subprocess.run([
    "nix", "--extra-experimental-features", "nix-command",
    "show-config", "--json"
], capture_output=True).stdout)

print(config.get("system", {}).get("value", "").removesuffix("-darwin"))
